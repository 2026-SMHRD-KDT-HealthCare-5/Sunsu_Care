// ==========================================================================
//  타이밍/지연 매직 넘버 중앙 관리
// ==========================================================================

/** BottomNav 홈 해시 스크롤 안정화 지연 */
export const HOME_HASH_SCROLL_DELAY_MS = 100;

/** 401 자동 로그아웃 중복 처리 차단 윈도우 (페이지 이동 전 다발 호출만 차단) */
export const AUTH_FAILURE_RESET_MS = 1000;
