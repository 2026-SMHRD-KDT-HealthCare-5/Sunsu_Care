{
  "name": "sunsu-care-root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "back": "npm run dev --workspace=backend",
    "front": "npm run dev --workspace=frontend",
    "dev": "concurrently \"npm run back\" \"npm run front\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.16.1",
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "python-dotenv": "^0.0.1-security"
  }
}
